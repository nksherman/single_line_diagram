import React from 'react';

import { Box } from '@mui/material';


function ReactFlowToPDFParser({ flowNode, pageSize, orientation }) {
  

  // with the copy of the flowNode

  // identify svg elements

  // identify div/elements

  

  
}

export default ReactFlowToPDFParser;